{
  "name": "paperclip-project-template",
  "version": "0.1.0",
  "private": true,
  "description": "Baseline gouvernée pour les projets Paperclip — à renommer après 'Use this template'",
  "scripts": {
    "dev": "echo 'À configurer selon le projet (vite, next dev, etc.)' && exit 1",
    "build": "echo 'À configurer selon le projet' && exit 1",
    "start": "echo 'À configurer selon le projet' && exit 1",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "security": "semgrep --config=auto --error",
    "check:fast": "pnpm run lint && pnpm run typecheck",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run test:coverage && pnpm run security"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@playwright/test": "^1.48.0",
    "@vitest/coverage-v8": "^2.1.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
